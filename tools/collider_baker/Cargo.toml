[package]
name = "collider_baker"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { workspace = true }
# Geometry only (convex hulls, VHACD) — the runtime physics engine was removed.
parry3d = "0.25"
bevy_mesh = "0.19.0"
shared = { path = "../../shared" }
serde = { workspace = true }
ron = "0.8"
bincode = "1.3"

