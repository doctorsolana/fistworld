[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { workspace = true }
lightyear = { workspace = true }
# Component visibility uses the backend already locked by Lightyear 0.28.
bevy_replicon = { version = "0.41.1", default-features = false, features = ["server", "client"] }
shared = { path = "../shared" }

parry3d = "0.25.3"
rand = "0.8"
serde_json = "1"
