app = 'fistforce'
primary_region = 'ams'

[build]

# UDP service for game traffic (not HTTP!)
[[services]]
  protocol = "udp"
  internal_port = 5000

  [[services.ports]]
    port = 5000

[env]
  CITYSIM_MAP_ID = "big_world"
  FISTWORLD_IMMIGRANTS_PER_DAY = "3"
  FISTWORLD_WORLD_NPC_CAP = "5000"
  RUST_LOG = "info"

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
